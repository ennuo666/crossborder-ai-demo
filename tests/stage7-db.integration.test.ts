import test from "node:test";
import assert from "node:assert/strict";
import { PrismaClient } from "@prisma/client";
import { createPrismaRepositories } from "@/repositories/prisma-repository";
import { createTask } from "@/services/task-service";
test("PostgreSQL persistence and reload", { skip: !(process.env.DATABASE_URL && process.env.RUN_DB_INTEGRATION === "1") }, async()=>{const client=new PrismaClient(); const repos=createPrismaRepositories(client); const product=await repos.products.create({name:`db-test-${Date.now()}`,market:"US",channel:"Amazon"}); const task=await createTask(product.id,"RESEARCH",undefined,repos); const fresh=createPrismaRepositories(new PrismaClient()); const loaded=await fresh.products.findById(product.id); const taskLoaded=await fresh.tasks.findById(task.id); const asset=await repos.assets.create({productId:product.id,taskId:null,kind:"image",name:"test",url:null,metadata:{},status:"ready"}); const seo=await repos.seoAudits.create({productId:product.id,taskId:null,score:80,issueCount:1,details:{},status:"completed"}); const freshAsset=await fresh.assets.listByProduct(product.id); const freshSeo=await fresh.seoAudits.latestByProduct(product.id); assert.equal(loaded?.id,product.id); assert.equal(taskLoaded?.status,"queued"); assert.equal(freshAsset.some(row=>row.id===asset.id),true); assert.equal(freshSeo?.id,seo.id); await client.$disconnect();});

